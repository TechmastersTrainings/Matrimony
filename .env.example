# ==========================================
# Christian Matrimony - Root Environment Configuration Template
# (Do NOT add real secrets here. Copy to .env for local usage.)
# ==========================================

# General Environment
ENVIRONMENT=development
DEBUG=True
APP_NAME="Christian Matrimony API"
API_V1_STR=/api/v1
CORS_ORIGINS=["http://localhost:3000","http://localhost:3001","http://localhost:8000"]

# Database (Aiven MySQL / Managed MySQL)
DATABASE_URL=mysql+pymysql://<DB_USER>:<DB_PASSWORD>@<DB_HOST>:<DB_PORT>/<DB_NAME>?ssl-mode=REQUIRED
DB_POOL_SIZE=10
DB_MAX_OVERFLOW=20
DB_POOL_TIMEOUT=30
DB_POOL_RECYCLE=1800

# Redis (Upstash Redis)
REDIS_URL=rediss://<REDIS_USER>:<REDIS_PASSWORD>@<REDIS_HOST>:<REDIS_PORT>
REDIS_TOKEN=your_upstash_redis_token_here

# Object Storage (Cloudflare R2)
R2_ACCOUNT_ID=your_cloudflare_r2_account_id
R2_ACCESS_KEY_ID=your_cloudflare_r2_access_key_id
R2_SECRET_ACCESS_KEY=your_cloudflare_r2_secret_access_key
R2_BUCKET_NAME=christian-matrimony-media

# Add after the actual matrimony domain is purchased
# and the R2 custom domain is configured.
R2_PUBLIC_URL=

# Monitoring & Logging
SENTRY_DSN=
LOG_LEVEL=INFO

# Future Service Placeholders
INDIAN_SMS_PROVIDER_API_KEY=
UPI_PAYMENT_GATEWAY_KEY=
UPI_PAYMENT_GATEWAY_SECRET=
FIREBASE_CREDENTIALS_PATH=
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
